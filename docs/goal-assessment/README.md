# CLI-UX-REFLECT: Assessment of finished CLI-UX goal

This docs subdir will contain a full review and assessment of the original CLI-UX goal.

All original docs from the CLI-UX workflow live in "docs/goal/**".

All code changes for the _final_ state of the CLI-UX goal now live in the `prep-goal` branch, which is our current branch as of this writing.

None of the changes have been merged into main.