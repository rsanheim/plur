[project]
name = "plur-docs"
version = "0.1.0"
description = "Documentation for Plur parallel test runner"
requires-python = ">=3.14"
dependencies = [
    "zensical==0.0.59",
    "linkcheckmd>=1.4.0",
]
