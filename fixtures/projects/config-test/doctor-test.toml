workers = 4
color = true
use = "doctor-test"

[job.doctor-test]
description = "Custom test job for doctor verification"
cmd = ["echo", "DOCTOR TEST TASK"]
watch_dirs = ["spec", "lib"]
target_pattern = "spec/**/*_spec.rb"
