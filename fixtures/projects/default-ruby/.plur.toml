# Plur configuration for default-ruby test fixture
# Using direct rspec command for 12.5% performance improvement
# Old format for main branch compatibility
command = "rspec"

# New format for interactive-config branch
[task.rspec]
run = "rspec"