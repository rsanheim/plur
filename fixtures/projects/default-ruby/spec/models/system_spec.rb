require "spec_helper"

RSpec.describe "System tests", type: :system do
  it "renders the homepage" do
    expect(true).to be(true)
  end

  it "handles user login flow" do
    expect(true).to be(true)
  end
end
